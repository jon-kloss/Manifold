# BACKLOG

Everything tempting that isn't Phase 1. Tags: v1 (later phase of this release), v1.1, v2.

- v1: Full community world-node snapshot (all nodes, all purities, geysers) replacing the Phase 1 subset — pending tile/data licensing review.
- v1: Community map tile set (3 zoom levels) once licensing is verified (SDD §7).
- ~~v1: Inter-factory routes + empire recompute + audit drawer~~ — DONE (Phase 2).
- ~~v1: Circuits, generator factories~~ — DONE (Phase 2); priority switches still open (needs persisted Circuit entities).
- ~~v1: Proposal system, global solver, wizard, T2 mini-proposals~~ — DONE (Phase 3).
- ~~v1: Rail/truck/drone inspectors + save import~~ — DONE (Phase 4).
- ~~v1: AI layer — advisor gate, chat, proposal_intent, style guides~~ — DONE (Phase 5, offline-honest; model relay scaffolded).
- v1.1: Batteries/storage in circuit math (`⚡ +BUFFER` slot reserved, A2.2).
- v2: Signal-block rail capacity modeling (A3.1); direct .pak icon/tile extraction (SDD §7).
- v1: Belt tier picker on connection drop (currently defaults Mk.1, editable via inspector/edge select) — UI spec 4c.
- ~~v1: Right-drag route drawing from pins on the map~~ — DONE (Phase 2); drawing from *nodes* still open.
- v1: "Plan a move" flow for built factory pins (built layer arrives with import).
- v1.1: True shared-element map↔factory zoom morph (currently scale+fade).
- v1.1: Node index numbers on map labels (`FE PURE #08` style) once the full node snapshot lands.
- v1.1: Belt corridor bundling (shared-lane routing when many belts run parallel) and manual waypoint pins on belts.
- v2: Machine footprint dimensions from pak extraction replacing the community table in `footprints.ts`.
- ~~v1: Supplemental generator fluids (water for coal/nuclear) — lands with pipes.~~ — DONE: coal 45, nuclear 240 m³/min ride the synthesized burn recipes as ordinary fluid inputs (`mSupplementalToPowerRatio × MW × 60/1000`), solved from Water Extractor / pump recipes. Inter-factory `RouteKind::Pipe` routing shipped too: water must be piped from a Water Extractor to a generator factory for it to produce power (planned unrouted fluid IN ports read 0; ◆ built plants assume untraced water). See DECISIONS.md "Routed water".
- v1.1: Route waypoint editing (paths are multi-point in the model; UI draws straight pin-to-pin).
- v1.1: Persisted Circuit entities: user naming, breakers, priority switches (grids are derived-only today).
- v1.1: Pipe head-lift math from route climb (z groundwork landed; pipe routes now exist — head-lift deliberately deferred: a pipe's flat 300/600 m³/min tier cap ignores vertical climb for now).
- v1: Rail grade warnings from per-segment slope (z groundwork landed; needs Phase 4 rail).
- v1.1: Elevation from a licensed heightmap (auto-fill pin z + climb along interior waypoints) — replaces planner-entered z.
- v1.1: True cross-item MILP recipe selection (per-item scoring today; matters once alternates create trade-offs).
- v1.1: Multi-site decomposition in the global solver (one integrated site per solve today); EXPAND-vs-GREENFIELD preference currently only expands generators.
- v1.1: Proposal detail popover (BEFORE→AFTER table, mock 3b) + EDIT VALUES on items; D diff key.
- v1.1: Unlock/progression model (milestones + hard drives) — alternates render locked until then.
- v1.1: Multi-item rail manifests (needs multi-port route binding; math block already sums the manifest).
- v1.1: Belt-bridged cluster merging + belt/rail/power route reconstruction from save connection components (counts imported today).
- v1.1: Truck fuel + drone batteries injected as solver demand (rendered as line items today).
- v2: Signal-block rail capacity modeling (fixed 15% headway today, A3.1).
- v1.1: Streaming model relay (SSE) behind FICSIT_AI_KEY + OS-keychain storage; advisor prose pass over armed events within the visible budget.
- v1.1: Image→style-guide vision call (surface + entity are live; needs the model relay).
- v1.1: An empty upstream factory feeds routes unconstrained (no groups → no supply entry); tighten to zero-supply once boundary-only factories have a defined meaning.
- v1.1: @-entity references in the chat composer; chat history persistence.
- **v1.1 — drift-sync rewiring:** `UpdateGroup` sync ops adjust group counts but don't refresh the factory's auto-wired edges/boundary-port rates, and a group newly added by drift isn't wired at all. Re-run the wiring pass over a factory after its sync ops apply.
- **v1.1 — physical belt connectivity:** the .sav does contain conveyor connections; parsing them would replace inferred wiring with ground truth (big parser lift, quarantine interactions).
- **v1.1 — richer built-group deltas:** structural/demolition deltas on ◆ groups (recipe swap, delete, count→0), solver/target-drag-generated deltas on built factories, and DIFF-tab per-delta rows (the "◇ Δ#7 — PENDING BUILD" chip, ui-spec 2). Count/clock overlay landed with SDD §3.1.1; delta-bearing groups currently count into the PLAN DRIFT row.
- **v1.1 — drift-sync dangling edges on group removal:** `UpdateGroup { count: 0 }` (single group demolished inside a surviving factory) removes the group but leaves belt edges whose `EdgeEnd::Group` now dangles; drop edges touching the removed group in that `apply_sync` arm.
- **v1.1 — non-Node resource sites:** fracking wells (core + satellites, per-satellite purity), geysers, and cave/entrance zoning for the real dataset — the vendored extraction has them but `gen-world-nodes.py` excludes what the extractor model doesn't cover yet. Cave rendering + schema already support `zone`/`entrance`.
- **v1.1 — W1c milestone build progress:** the dashboard's "built / total" uses gross ◆ production RATE of the milestone item as the "built" figure (the save exposes no lifetime-crafted quantity); swap for a true cumulative count if save parsing ever surfaces one.
- **v1.1 — W1c route/claim auto-detection:** route and node-claim build steps are manual-mark-only (belt connectivity is invisible to the save); auto-derive their completion once physical belt connectivity parsing lands (see the physical-belt-connectivity item above).
- **v1.1 — W1c resume dashboard re-greet:** the dashboard greets once per plan (persisted `viewState.resumeSeen`); re-greeting at the start of a genuinely new session (fresh day / app relaunch) rather than only once-ever is a UX enhancement.
- **v1.1 — W2a multi-factory refactor:** v1 links a single ◇ replacement to a single ◆ factory via `replaces`; multi-factory consolidation (many ◆ → one ◇), group-granular cutover, and partial cutover (replace only part of a factory) are deferred — the projection and downtime engine assume one-to-one.
- **v1.1 — W2a player-entered step durations:** cutover downtime wall-clock is a machine-count estimate (`SWITCH_MIN_PER_MACHINE`, labeled "(est)"); let the player enter real per-step durations once they've done a few cutovers.
- **v1.1 — W2a belt-repoint as first-class members:** the Switch phase is manual-only steps ("repoint the belts") because belt connectivity is invisible to the save; v1 has the player edit routes between steps by hand. Model the belt/route re-point as first-class cutover members (with derived completion) once physical connectivity parsing lands.
- **v1.1 — W2a downtime-minimizing order:** the cutover order is fixed BuildNew → Switch → Dismantle; a solver that reorders/interleaves steps (or stages partial switch-overs) to minimize the downtime dip is deferred.
- **v1.1 — W2a in-place recipe swap:** a whole-factory replacement is the v1 refactor; in-place recipe/tier swap on a ◆ factory via structural ◆ deltas (demolition/rebuild deltas) is deferred alongside the richer built-group deltas item above.
- W2a v1.1 — scoped replacement: let the player choose which output product-line(s) of a multi-output factory to replace (avoids atomic mega-cluster infeasibility); pairs with the multi-entity cutover model.
- **v1.1 — Geysers/resource wells in the world snapshot:** purity-scaled geothermal siting + output replace the nameplate 200 MW average (pairs with the non-Node resource sites item above).
- **v1.1 — import reconciliation for claim offers (PR #9 review):** an unmapped save extractor falls to a `save:<id>` plan-local claim, leaving the catalog node it physically sits on unclaimed — so `untapped_node` can offer an already-mined node (one solid-UAID miner observed on the megabase probe); reconcile imported extractors against catalog nodes before surfacing offers.
- **v1.1 — W2b-C fracking/geyser reconciliation:** node binding covers miners on `BP_ResourceNode_C` catalog nodes; fracking wells (core + per-satellite) and geysers stay deferred (they aren't in the bundled catalog yet — see the non-Node resource sites item), so their extractors fall to `save:<id>` plan-local claims.
- **v1.1 — W2b-C solver honors corrected geometry:** the plan-local node position overlay corrects DISPLAY + claim tethers; having the solver honor the corrected coordinate in extraction ceilings / route lengths is a follow-up (v1 corrects display + claims, extraction rate stays catalog-purity-driven).
- **v1.1 — W2b-D cross-item MILP optimizer:** the empire alt-optimizer ranks each product's alternate independently (greedy per-item); a true cross-item MILP that captures shared-intermediate trade-offs (an alt that trades iron for steel shifts steel demand, changing steel's own optimum) is deferred.
- **v1.1 — W2b-D one-click adopt-across-all-factories:** a single batch "adopt this alt everywhere" that co-plans one consolidated cutover across every affected ◆ factory (needs the multi-factory single-cutover model) instead of drafting a Refactor proposal per built factory.
- **v1.1 — W2b-D locked-alt "unlock this to save X" suggestions:** the optimizer ranks only UNLOCKED alternates (actionable now); ranking genuinely-LOCKED alternates as "unlock this hard drive to save N machines / M MW" aspirational suggestions is deferred.
- **UX — a source exporting to WORLD reads 0 until its OUT-port target is set:** a placed Water/Resource-Well Extractor whose only sink is a "TO WORLD" export idles at 0 m³/min because a world export creates no demand to pull production; the user must manually set the OUT port's target rate. (Observed 2026-07-21.) A source (zero-ingredient extraction group) with an unbound/world-exported OUT port should default that port's target to the group's nameplate so it produces immediately — mirrors how generators are driven to nameplate. `ClaimWell` already sizes its OUT port to production; the general fix is the add-machine / add-port path.
